---
post:
  operationId: /auth/login
  tags:
    - auth
  requestBody:
    description: 登入
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas.yml#/schemas/authLoginRequest"
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../schemas.yml#/schemas/authLoginResponse"