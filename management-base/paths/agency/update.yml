---
patch:
  operationId: /agency/update
  description: 更新代理
  tags:
    - agency
  parameters:
    - in: path
      name: agencyId
      description: agency ID
      required: true
      schema:
        type: integer
        format: int64
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas.yml#/schemas/agencyUpdateRequest"
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../schemas.yml#/schemas/agencyUpdateResponse"