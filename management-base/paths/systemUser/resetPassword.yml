---
patch:
  operationId: /systemUser/resetPassword
  description: 系統用戶重設密碼
  tags:
    - systemUser
  parameters:
    - in: path
      name: systemUserId
      description: system user ID
      required: true
      schema:
        type: integer
        format: int64
  requestBody:
    description: 系統用戶重設密碼
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas.yml#/schemas/systemUserResetPasswordRequest"
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../schemas.yml#/schemas/systemUserResetPasswordResponse"