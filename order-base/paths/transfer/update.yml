---
patch:
  operationId: /transfer/update
  description: 更新轉帳
  tags:
    - transfer
  parameters:
    - in: path
      name: transferId
      description: transfer ID
      required: true
      schema:
        type: integer
        format: int64
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas.yml#/schemas/transferUpdateRequest"
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../schemas.yml#/schemas/transferUpdateResponse"