---
post:
  operationId: /user/update
  description: 用戶更新
  tags:
    - user
  parameters:
    - in: path
      name: userId
      description: user ID
      required: true
      schema:
        type: integer
        format: int64
  requestBody:
    description: 用戶更新
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas.yml#/schemas/userUpdateRequest"
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../schemas.yml#/schemas/userUpdateResponse"