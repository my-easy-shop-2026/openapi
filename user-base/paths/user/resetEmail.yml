---
post:
  operationId: /user/resetEmail
  description: 用戶重設email
  tags:
    - user
  parameters:
    - in: path
      name: userId
      description: user ID
      required: true
      schema:
        type: integer
        format: int64
  requestBody:
    description: 用戶重設密碼
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas.yml#/schemas/userResetEmailRequest"
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../schemas.yml#/schemas/userResetEmailResponse"