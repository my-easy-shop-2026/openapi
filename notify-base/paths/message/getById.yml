---
get:
  operationId: /message/getById
  description: 取得通知紀錄
  tags:
    - message
  parameters:
    - in: path
      name: messageId
      description: send Record ID
      required: true
      schema:
        type: integer
        format: int64
  # requestBody:
  #   required: true
  #   content:
  #     application/json:
  #       schema:
  #         $ref: "../../schemas.yml#/schemas/messageGetByIdRequest"
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../schemas.yml#/schemas/messageGetByIdResponse"